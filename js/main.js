document.addEventListener('DOMContentLoaded',function(){
  document.getElementById('year').textContent = new Date().getFullYear();

  // Interactive contact form removed — contact is now static on the page

  // XSLT transform demo
  // XML/XSLT demo removed — no client-side transform
});
